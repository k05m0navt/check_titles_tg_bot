# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Supabase Configuration
SUPABASE_URL=your_supabase_project_url
SUPABASE_KEY=your_supabase_api_key

# Database Connection (for migrations)
# Get from: Supabase Dashboard → Settings → Database → Connection string → URI
# Format: postgresql://postgres:[YOUR-PASSWORD]@[PROJECT-REF].supabase.co:5432/postgres
# Important: If your password contains special characters (#, @, %, etc.), 
#            the migration script will automatically URL-encode them for you.
#            You can use the password as-is, just make sure to wrap it in quotes if needed:
#            DATABASE_URL="postgresql://postgres:#M#83bNY63NAGasDXrmp@project.supabase.co:5432/postgres"
DATABASE_URL=postgresql://postgres:your_password@your_project_ref.supabase.co:5432/postgres
# Alternative: If you prefer to keep password separate (password will be auto-encoded)
# DATABASE_PASSWORD=your_database_password

# Admin Configuration (at least one required)
ADMIN_USER_ID=your_telegram_user_id_here
# Optional: Telegram username without @ symbol (e.g., "john_doe" not "@john_doe")
# Only needed as fallback if ADMIN_USER_ID is not set
ADMIN_USERNAME=your_telegram_username_here

# Google Sheets Migration (migration only - can be removed after migration)
GOOGLE_SHEET_ID=your_google_sheet_id
GOOGLE_CREDENTIALS=your_google_credentials_json_string
